# Guided Spring Boot Project

This repository is to store my progress on this tutorial: [Building an Application with Spring Boot](https://spring.io/guides/gs/spring-boot).

This project uses the maven build tool. Relevant commands:
- ./mvnw spring-boot:run (running the application)
- curl http://localhost:8080 (running the service with curl)
- s
